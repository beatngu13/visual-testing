# Due to different paths on local machine and CI server.
attribute=background-image
# clip: expected="rect(1px, 1px, 1px, 1px)", actual="rect(1px 1px 1px 1px)".
attribute=clip
# font-family: expected="system-ui", actual="Arial"
attribute=font-family
# Currently to fragile.
attribute=outline
